import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // BFF proxy handles API calls
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'res.cloudinary.com' },
      { protocol: 'https', hostname: 'images.unsplash.com' },
    ],
  },
};

export default nextConfig;
